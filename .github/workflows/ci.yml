name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:

permissions:
  contents: read

jobs:
  unit-tests:
    name: Unit Tests
    runs-on:
      - self-hosted
      - ci

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Prepare log directory
        run: mkdir -p .ci-logs/unit

      - name: Run unit tests
        shell: bash
        run: |
          chmod +x gradlew
          ./gradlew clean test 2>&1 | tee .ci-logs/unit/unit-tests.log

      - name: Upload unit test logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-logs
          path: .ci-logs/unit/**
          if-no-files-found: warn
          retention-days: 14

  integration-tests:
    name: Integration Tests
    runs-on:
      - self-hosted
      - ci
    needs:
      - unit-tests

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Prepare log directory
        run: mkdir -p .ci-logs/integration

      - name: Run integration tests
        shell: bash
        env:
          COMPOSE_PROJECT_NAME: ttrpg-service-ci
          INTEGRATION_BASE_URL: http://localhost:8080
          LOG_DIR: .ci-logs/integration
          DOCKER_SUDO: "true"
          POSTGRES_DB: ttrpg_tests_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          DB_FLYWAY_USER: flyway_user
          DB_FLYWAY_PASSWORD: flyway_pass
          DB_NAME: ttrpg_tests_db
          DB_HOST: postgres
          DB_PORT: 5432
          DB_USER: ttrpg_tests_app_user
          DB_PASSWORD: ttrpg_tests_app_pass
          DB_SCHEMA: ttrpg_assistant_tests_app
        run: |
          chmod +x gradlew
          bash scripts/run-integration-tests.sh

      - name: Upload integration logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-logs
          path: .ci-logs/integration/**
          if-no-files-found: warn
          retention-days: 14
